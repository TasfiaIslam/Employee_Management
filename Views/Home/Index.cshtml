@model IEnumerable<Employee>

@{
    ViewBag.Title = "Employee List";
}
<br />
@*<img class="img-fluid img-full-width" src="~/images/banner2.svg" alt="WorkPlace">*@
@*<img name="slide" width="1200" height="600" />*@
<div class="card-deck">
    @foreach (var employee in Model)
    {
        var photoPath = "~/Images/" + (employee.PhotoPath ?? "noimage.jpg");
        <div class="card m-3" style="min-width:18rem; max-width:30.5%">
            <div class="card-header">
                <h3>@employee.Name</h3>
            </div>
            <img class="card-img-top imageThumbnail" src="@photoPath" asp-append-version="true" />
            <div class="card-footer text-center">
                <a asp-controller="Home" asp-action="Details" asp-route-id="@employee.Id" class="btn btn-primary m-1">View</a>
                <a asp-controller="Home" asp-action="Edit" asp-route-id="@employee.Id" class="btn btn-primary m-1">Edit</a>
                <a href="#" class="btn btn-danger m-1">Delete</a>
            </div>
        </div>
    }
</div>
@*@section Scripts {
    <script>
        var i = 0;
        var images = [];
        var time = 5000;

        images[0] = '~/images/banner7.jpg';
        images[1] = '~/images/banner8.jpg';

        function changeImage() {
            document.slide.src = images[i];

            if (i < images.length - 1) {
                i++;
            }
            else {
                i = 0;
            }

            setTimeout("changeImage()", time);
        }
        window.onload(changeImage);
    </script>
}*@